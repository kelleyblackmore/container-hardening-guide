#!/bin/bash
# Placeholder referenced by the anti-pattern Dockerfile so the COPY resolves.
# This image is never meant to be built for deployment - only linted and
# scanned. See examples/insecure/Dockerfile.
echo "this image is an anti-pattern demo; do not deploy it"
